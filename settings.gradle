rootProject.name='conductor-2.22.4.34'

include 'client','common','contribs','core', 'es6-persistence','jersey', 'postgres-persistence', 'zookeeper-lock', 'redis-lock'
include 'cassandra-persistence', 'mysql-persistence', 'redis-persistence','server','test-harness','ui','ui-admin'
include 'grpc', 'grpc-server', 'grpc-client'

rootProject.children.each {it.name="conductor-${it.name}"}

